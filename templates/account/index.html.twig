{% extends 'base.html.twig' %}

{% block title %}Mon compte - JHADBoutique{% endblock %}

{% block body %}
<div class="container w-75">

    <h1>Mon compte</h1>

    Bienvenu {{ app.user.firstname}} dans votre compte. <br> C'est dans cet espace que vous allez pouvoir gérer toutes vos informations personnelles<br>
    <hr>

    {% if is_granted('ROLE_ADMIN') %}
        <!-- <a href=""> Vous êtes un administrateur, acceder au back office ? </a> --><br>
    {% endif %}

    {% if not app.user.image is empty %}
        <img src="{{asset('uploads/images/profil/'~app.user.image)}}" height="60px" />
    {% endif %}
    <span>{{ app.user.firstname}} {{ app.user.lastname}}</span></br>
    <span> <small class="font-weight-bold">Date de naissance : </small>{{ app.user.birthday|date("d/m/Y")}}</span></br>
    <span> <small class="font-weight-bold">Votre email : </small>{{ app.user.email}}</span></br>
    <span><small class="font-weight-bold">Votre telephone : </small>{{ app.user.phone}}</span> </br>
    <span><small class="font-weight-bold">Votre adresse : </small>{{ app.user.address}} {{ app.user.zipcode}} {{ app.user.city}} </span></br>
    <span><small class="font-weight-bold">Votre Pays : </small>{{ app.user.country}}</span>

    <hr>

    <div class="row">

        <div class=" col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/image/icone/padlock.png') }}" alt ="Modifier mon mot de passe" height="20px">
                <a href="{{ path('account_password') }}"> Modifier mon mot de passe</a>
            </div>
        </div>
        <div class=" col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/image/icone/rdv.png') }}" alt ="Mes rendez-vous" height="20px">
                <a href="{{ path('calendar_index') }}">Mes rendez-vous</a>
            </div>
        </div>
        <div class=" col-md-4">
            <div class="account-item text-center">
                <img src="{{ asset('assets/image/icone/shopping-bag.png') }}" alt ="Mes commandes" height="20px">
                <a href="">Mes commandes</a>
            </div>
        </div>


    </div>
    <hr>
</div>

{% endblock %}
