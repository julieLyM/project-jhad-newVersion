{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - JHADBeauty{% endblock %}

{% block body %}
<h3 class="text-center mt-4">Modifier mon mot de passe</h3>
<div class="container w-50">{% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

<a href="{{ path('account') }}">Retourner à mon compte</a><br>
    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
</div>
{% endblock %}