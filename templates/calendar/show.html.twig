{% extends 'base.html.twig' %}

{% block title %}Calendar{% endblock %}

{% block body %}
    <h1>Calendar</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Reférence</th>
            <td>{{ calendar.id }}</td>
        </tr>
        <tr>
            <th>Prestation</th>
            <td>{{ calendar.product.name }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ calendar.start ? calendar.start|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        {# <tr> #}
        {# <th>End</th> #}
        {# <td>{{ calendar.end ? calendar.end|date('Y-m-d H:i:s') : '' }}</td> #}
        {# </tr> #}
        <tr>
            <th>Spécifications</th>
            <td>{{ calendar.description }}</td>
        </tr>
        {# <tr> #}
        {# <th>All_day</th> #}
        {# <td>{{ calendar.allDay ? 'Yes' : 'No' }}</td> #}
        {# </tr> #}
        {# <tr> #}
        {# <th>Background_color</th> #}
        {# <td>{{ calendar.backgroundColor }}</td> #}
        {# </tr> #}
        {# <tr> #}
        {# <th>Border_color</th> #}
        {# <td>{{ calendar.borderColor }}</td> #}
        {# </tr> #}
        {# <tr> #}
        {# <th>Text_color</th> #}
        {# <td>{{ calendar.textColor }}</td> #}
        {# </tr> #}
        </tbody>
    </table>

    <div class="d-flex">
    <button class="btn btn-success btn-sm">
        <a href="{{ path('calendar_new') }}">Ajouter une réservation</a>
    </button>
    </div>
    <a href="{{ path('calendar_index') }}">Retour à la liste</a>

{% endblock %}
