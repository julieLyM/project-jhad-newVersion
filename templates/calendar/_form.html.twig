{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm" style="background-color: lightcoral; color: rebeccapurple; font-weight:500">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
